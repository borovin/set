machine:
  node:
    version: stable

dependencies:
  post:
    - npm install karma-junit-reporter codacy-coverage

test:
  override:
    - npm test -- --reporters=junit,mocha
  post:
    - mv reporters/junit.xml $CIRCLE_TEST_REPORTS